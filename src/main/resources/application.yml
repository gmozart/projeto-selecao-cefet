server.port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cefet?useTimezone=true&serverTimezone=UTC&useLegacyDate
    username: postgres
    password: root
  jpa:
    show-sql: true